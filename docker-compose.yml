services:
  db:
    image: postgres:18
    container_name: example-db
    environment:
      POSTGRES_DB: mydb
      POSTGRES_USER: nammon
      POSTGRES_PASSWORD: 14092545
    ports:
      - "5432:5432"
    volumes:
      - postgres_data:/var/lib/postgresql/data

volumes:
  postgres_data: